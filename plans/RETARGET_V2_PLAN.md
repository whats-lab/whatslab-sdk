# 리타게팅 v2 실행 계획

방법론 정본은 `ref/planing.md`. 이 문서는 **실행 상태**만 기록한다 — 무엇이 끝났고,
무엇이 새 방법론으로 폐기됐고, 다음에 무엇을 어떤 판정 기준으로 재는가.

작업 브랜치 `feat/geort-retarget`. kp 구현은 `feat/kp-hand-retargeter` 에 고정(비교 기준선).
실험 코드는 저장소 밖 `/home/whatslab09/geort-lab` (GeoRT 벤더링, CC-BY-NC).

---

## 0. 확정 기준선 — **왼손** (`calib_base_left_20260810_154406.json`)

`tools/bench_hand_retarget.py --traj pinch|flex|abd`. GMC/LMC 는 평균 코사인×100,
격차 = LMC − GMC. **측정 전 `find . -name __pycache__ -type d -exec rm -rf {} +`** —
stale 바이트코드로 재현 불가한 런이 나왔다.

**측정 조건**: models root = `/home/whatslab09/whatslab-models/dexhand_description`
(2026-08-11 사용자 수정 반영본). **`regenerate.py` 를 돌리면 이 표가 무효가 된다** —
Visualizer 소스로 덮어써서 사용자 수정을 날린다. 절대 실행하지 않는다.

| 궤적 | 설정 | 형상° | 벌림mm | 핀치접촉 | 펴짐접촉 | GMC | LMC | 격차 | \|dq\|p95 | ms |
|---|---|---|---|---|---|---|---|---|---|---|
| pinch | orca kp | 19.8 | 3.4 | 11.5 | 1.9 | 98.8 | 89.5 | −9.3 | 0.097 | 2.3 |
| pinch | orca dex | 17.6 | 5.5 | 16.8 | 35.0 | 85.6 | 82.0 | −3.6 | 0.150 | 5.9 |
| pinch | robotis kp | 14.3 | 4.2 | 5.5 | 4.5 | 98.7 | 92.3 | −6.4 | 0.099 | 1.9 |
| pinch | robotis dex | 13.6 | 6.5 | 20.2 | 32.4 | 81.2 | 79.0 | −2.2 | 0.155 | 5.1 |
| flex | orca kp | **48.0** | **13.5** | – | 1.4 | 96.4 | **58.1** | **−38.3** | 0.156 | 2.4 |
| flex | orca dex | 44.1 | 14.7 | – | 32.9 | 92.4 | 68.7 | −23.7 | 0.116 | 8.8 |
| flex | robotis kp | **34.0** | **11.8** | – | 5.0 | 95.3 | 63.4 | −32.0 | 0.135 | 1.9 |
| flex | robotis dex | 17.5 | 16.9 | – | 10.5 | 86.2 | 76.0 | −10.2 | 0.130 | 6.1 |
| abd | orca kp | 11.2 | 3.3 | – | 11.7 | 90.4 | 72.6 | −17.8 | 0.054 | 2.4 |
| abd | orca dex | 10.6 | 4.6 | – | 18.4 | 75.8 | 58.7 | −17.1 | 0.148 | 6.0 |
| abd | robotis kp | 8.4 | 5.2 | – | 7.1 | 99.6 | 67.4 | −32.2 | 0.039 | 2.0 |
| abd | robotis dex | 14.5 | 7.7 | – | 13.4 | 80.1 | 62.6 | −17.5 | 0.175 | 6.4 |

**읽는 법 세 줄.**

1. **핀치만 보면 kp 가 아주 좋아 보인다** (형상 14~20°, LMC 90, 격차 −6~−9). 실제 불만이
   핀치에서 안 보이는 이유가 이것이다.
2. **굽힘 전용에서 무너진다** — orca kp 형상 19.8 → 48.0°, 벌림오차 3.4 → 13.5mm,
   LMC 89.5 → 58.1. 굽힘만 하는 입력인데 손가락 간격과 뼈 방향이 사람을 못 따라간다.
   이것이 "굽힐 때 좌우로 흔들린다"의 정량화다. **핀치 궤적만으로 판정하면 안 된다.**
3. **격차(LMC−GMC)가 진단값이다.** kp 는 GMC 90~99 로 높은데 LMC 58~92 로 낮다
   (최악 orca 굽힘 −38.3). 전역 방향은 맞는데 **손가락 자기 프레임에서 보면 방향이 다르다**
   = 뼈축 기준으로 굴러가는 성분이 크다. 조작감 불만의 좌표계가 LMC 쪽이다. dex 는 GMC 가
   낮은 대신 격차가 작다(−2.2~−23.7) — 전역 정렬이 나쁘지만 국소적으로는 덜 뒤틀린다.
   **두 백엔드의 실패 방식이 다르다.**

**URDF 가 바뀌면 이 표를 다시 잰다.** 2026-08-11 사용자 수정으로 robotis 핀치접촉이
10.7 → 5.5mm 로 좋아지고 굽힘 형상은 둘 다 나빠졌다(orca 43.4 → 48.0, robotis 22.9 → 34.0).
사람 손 기하가 바뀌면 모든 지표가 움직인다.

### 0.1 좌우 미러 실측 (통합 판단의 전제)

`tools/check_mirror.py`. 같은 q 를 좌우에 넣고 팜프레임 로컬 사슬 좌표를 `(x, y, -z)` 로
비교한다. **두 실패 모드를 분리해서 잰다** — 섞으면 중립 오프셋이 모든 관절을 오염시켜
전 관절 실패로 보인다(실제로 처음에 robotis 를 20/20 으로 오판했다).

2026-08-11 사용자 URDF 수정 후:

| | 중립형상 | 축 미러오차>2mm | 최악 | 어긋난 관절 |
|---|---|---|---|---|
| human | **0.01mm** (was 4.77) | 4/21 | 83.1mm | `{index,middle,ring,pinky}_mcp_abd` |
| orca | 0.28mm | 3/17 | 11.2mm | `{I,M,P}-AP_*_to_*Carpals*` (외전축) |
| robotis | **7.83mm** (was 9.79) | 8/20 | 109.9mm | `finger_?_joint{1,2,5,9,13,17}` 계열 |

- **중립형상** = q=0 에서의 기하 비대칭(링크 원점·길이가 좌우로 다름). human 은 고쳐졌다.
- **축 미러오차** = 중립을 뺀 변위의 미러 불일치. 회전축 부호/방향 문제만 남는다.
  human 은 정확히 `*_mcp_abd` 4개 — `axis="0 0 1"` 이 좌우 동일한데 축은 의사벡터라
  미러 시 `a' = -M a` 로 부호가 바뀌어야 한다. 한쪽 4개만 반전하면 0.00mm 가 된다.

이전 세션 기록 두 건은 **틀렸다**: "robotis 는 완전한 미러(0.0mm) / orca 는 35~45mm
어긋남"은 지문 위치만 옛 URDF 로 잰 값, "robotis 20/20"은 중립 오프셋 미분리. 위 표가 정본.

## 1. 완료

**지표·궤적 (P0-2 완료)** — `tools/bench_hand_retarget.py`
- **GMC(전역) + LMC(로컬) + 격차**를 함께 낸다. GMC 는 공유 팜 프레임에서 변위 방향
  비교, LMC 는 손가락별 로컬 프레임(마지막 뼈 방향을 z, 팜 y 와의 외적을 x)으로 옮긴 뒤
  비교. **격차가 정렬 품질의 진단값**이고 실제로 그렇게 작동한다(§0 읽는 법 3).
- 부호 일치율만 쓰면 핀치 램프가 단조라서 kp 가 100% 로 포화한다 → 평균 코사인을 쓴다.
- `--traj pinch|flex|abd`. 굽힘·벌림 전용 스윕을 덤프 `theta_lo`/`theta_hi` 로 합성한다.
  **이 궤적 없이는 미해결 문제가 측정에 안 나타난다.**
- `tools/check_mirror.py` — 좌우 통합 게이트(§0.1).

**Phase 1 구조 부품 (구현·테스트 완료, 학습 전)**
- `solvers/hand/keyvector.py` — dorsum 원점, 손가락방향 축, 사슬 50% prox, `L_ref` 상수.
  사슬 유도는 kp 와 같은 `sensor_chains` 를 쓴다 → 팜 회전이 kp 와 `0.00e+00` 일치
  (두 백엔드를 공정하게 비교하려면 프레임이 같아야 한다).
- `solvers/hand/fk_torch.py` — pinocchio FK 를 `autograd.Function` 으로. 수치 미분과
  1e-5 이내 일치를 테스트로 고정. `idx_q`(형상)/`idx_v`(속도) 분리.
- `solvers/hand/net_retargeter.py` — 손가락별 MLP, tanh→`joint_roms`. 추론에 FK 없음.
  `mirror_to` 로 좌우 통합 모델 자리를 미리 뚫어 뒀다.
- 테스트 133 passed / 6 skipped (기존 116 + 신규 17).

**사람 URDF 관절명 갱신** — Visualizer 현행(`mcp_flex`/`mcp_abd`/`thumb_cmc_flex`)으로
models root 재생성. 프로파일 21개 관절 전부 매칭.

## 2. 새 방법론으로 폐기된 것

- **f_s 3안 비교(NNS / 학습IK / kp DLS)** — 통합 구조(F 가 q 직접 출력)라 해당 없음.
  측정해 둔 NNS 특성(사전샘플 20배에 오차 19.2→14.8mm, 1↔2위 이웃 q 점프가 밀도와
  무관하게 1.1rad 고정)은 **NNS 를 쓰지 않는 근거**로만 남긴다.
- **`sensor_proximal` 프레임을 prox 로 쓰는 것** — prox 는 **사슬 50% 중앙**으로 바뀐다.
  센서 실장 위치는 손마다 통일 작업이 반복돼 임베디먼트 확장에 약하다.
- **`v_tip→prox` keyvector** — `v_dorsum→{distal,prox}` 로 대체(선형종속 제외).
- **진행 중이던 tip/tip_prox 학습 런** — 키포인트 정의와 FK 가 둘 다 바뀌어 무효.
  (두 번 세션 경계에서 죽기도 했다. 재실행은 새 정의로.)

## 2.5 좌우 모델을 통합할지 — 결론: **통합을 목표로 설계, 도입은 미러 게이트 통과 후**

**통합이 가능한 근거.** 좌우 URDF 가 참 미러라면 팜프레임 로컬에서 왼손 keyvector 는
오른손 keyvector 의 z 성분 부호를 뒤집은 것과 **정확히** 같다(§0.1 의 비교식이 그것이다).
관절 한계도 좌우 동일하고 관절명은 접두사만 다르다. 따라서

```
q_robot = F( mirror_z(keyvector_left) )     # 같은 F, 같은 q
```

가 근사가 아니라 **엄밀한 대칭**이다. 같은 q 를 미러 기구에 넣으면 미러 동작이 나온다는 것을
§0.1 이 측정으로 확인했다(미러가 성립하는 관절에서 오차 0.00mm).

**통합해야 하는 이유 — 대칭성이 공짜로 보장된다.** 비지도 학습은 해가 유일하지 않아
시드마다 흔들린다(GeoRT 는 Ability Hand LMC 36.2±11.2 를 보고했다). 좌우를 **따로**
학습하면 두 손이 서로 다른 국소해에 앉아 **조작자가 왼손과 오른손에서 다른 감각**을 느낀다.
지금 겪는 흔들림과 결이 같은 문제이고, 통합은 이걸 구조적으로 없앤다. 부수 이득으로
학습 데이터가 2배(좌우 글러브 시퀀스를 미러로 합침), 체크포인트·학습시간은 절반이 된다.

**그런데 지금은 도입할 수 없다.** §0.1 대로 human 축 4/21, orca 3/17, robotis 8/20 이
미러가 아니고 중립형상도 human 4.77 / robotis 9.79mm 어긋난다. 미러가 깨진 상태에서
통합하면 한쪽 손의 오차를 다른 쪽에 강제로 떠넘긴다.

**따라서**: Phase 1·2 는 **왼손 side 별 모델**로 진행한다. `tools/check_mirror.py` 가
게이트이고, **손 하나가 축 0/N + 중립형상 2mm 이하가 되면 그 손만 통합으로 전환**한다.
orca 는 중립이 이미 0.03mm 이고 축 3개만 남아 먼저 열릴 가능성이 높다. robotis 는
중립 9.79mm + 축 8개라 더 걸린다 — 손별로 따로 판단한다.

**미러가 영구히 안 되는 손이 나오면** (설계상 좌우가 진짜 다른 경우) 그 손만 side 별
모델을 유지한다. 통합은 최적화이지 전제가 아니다.

## 3. P0 — 이후 모든 판정의 전제

| | 항목 | 담당 | 판정 |
|---|---|---|---|
| P0-1 | dorsum 원점 통일 (6개 URDF) | URDF(Visualizer) | 좌우 미러오차 0.00mm, 손간 규약 일치 |
| P0-2 | **LMC** 구현 + GMC 병기 | 코드 | **완료** — 격차가 진단값으로 작동(§0) |
| P0-3 | pinocchio FK 배치 성능 | 코드 | 학습 배치에서 병목인지. 병목이면 pytorch_kinematics 병행 후 결과 대조 |

**P0-1 잔여 이슈 (URDF 쪽, 코드로 보정하지 않는다)** — 수용 기준은 `tools/check_mirror.py`.
1. **`*_mcp_abd` 4개 축** (human). `axis="0 0 1"` 을 한쪽에서 `"0 0 -1"` 로. 반전하면
   5·15·25° 전부 0.00mm.
2. **orca 외전축 3개**(`{I,M,P}-AP_*_to_*Carpals*`), 최악 11.2mm.
3. **robotis 중립형상 9.79mm + 축 8개** — 전 손가락 균일한 오프셋이라 체계적 원인이 있다.
4. **dorsum 위치 통일** (`ref/planing.md` III 표). robotis 좌우 50mm 불일치.
5. 동봉 패키지 `dexhand_description` 은 여전히 구버전 관절명 → `WHATSLAB_MODELS_ROOT`
   를 반드시 건다. 패키지 릴리스 시 갱신 필요.

**Phase 1·2 는 왼손 단일 side 라 1~4 가 열리지 않아도 진행할 수 있다.** 게이트는 좌우
통합 모델의 전제다.

## 4. Phase 1 — GeoRT 5원칙 + prox, 정확 FK  **(구현 완료 / 측정 결과 기각선 아래)**

`tools/train_hand_net.py --phase 1`. 왼손 robotis, epoch 107/200 중간 측정:

| 궤적 | | 형상° | 벌림mm | GMC | LMC | \|dq\|p95 |
|---|---|---|---|---|---|---|
| pinch | kp | 12.4 | 3.9 | 98.2 | 91.7 | 0.071 |
| pinch | **net** | **69.6** | **30.9** | **43.1** | 30.9 | 0.157 |
| flex | kp | 22.9 | 9.8 | 95.7 | 69.4 | 0.139 |
| flex | **net** | **44.9** | **26.1** | **41.1** | 32.1 | 0.353 |
| abd | kp | 13.5 | 4.4 | 99.4 | 58.7 | 0.038 |
| abd | **net** | **93.2** | **38.5** | **−37.9** | 13.8 | 0.081 |

(이 런의 `ms` 열은 학습이 CPU 를 같이 써서 무효. 학습 후 재측정한다.)

**벌림축 GMC 가 −37.9 다 — 사람이 벌리면 로봇이 반대로 간다.** 튜닝 문제가 아니라
구조적 실패다.

**원인.** GeoRT 5원칙에는 **사람 자세를 특정 로봇 자세에 묶는 항이 없다.**
motion(방향 일치)·coverage(도달영역 덮기)·flatness·pinch 뿐이고, 어느 것도 대응 관계를
고정하지 않는다. 비지도 해의 비유일성이 그대로 나온다 — `ref/planing.md` X-5 의 예측과
같다. 게다가 **motion 이 −0.21 에서 30 epoch 정체**했다(목표 −1.0):
`coverage 6.8e-3 × 80 = 0.54` vs `motion 0.21` 로 coverage 가 여전히 지배한다.
`w_coverage=80` 은 GeoRT 가 축별 [−1,1] 정규화에 맞춰 잡은 값인데 우리는 `L_ref`
하나로만 정규화하므로 스케일이 다르다.

**GMC 가 음수인 구간에서는 격차(LMC−GMC)를 읽지 말 것.** net 벌림축의 +51.7 은
GMC 가 −37.9 라서 나온 값이고 좋은 신호가 아니다. 전역 정렬이 양수 영역에 들어온
뒤에 격차를 본다.

**다음 두 단계** (순서 고정):
1. **가중치 스윕** — `--w-motion` 을 올리고 `--w-coverage` 를 내려 motion 이 실제로
   수렴하는지. 한 번에 하나만 바꾼다.
2. **Phase 2 `L_align`** — 대응을 고정하는 항은 이것뿐이다. 위 표가 앵커 기여를
   측정할 비교 기준선이고, 계획서가 Phase 1 을 비지도로 두라고 한 이유가 이것이다.

**P0-3 답**: FK forward+backward 12,000 samples/s (배치 256 에서 학습스텝당 85ms)
→ 200 epoch 약 7분. `pytorch_kinematics` 병행 불필요. 전부 CPU 라 GPU 부하가 없어
하드 리셋 위험도 없다. net 추론은 0.21ms (kp 1.89ms) — FK 없는 forward 1회.

## 5. Phase 2 — AnyDexRT 4손실 + anchor + residual affine  **(구현 완료 / 학습 대기)**

`tools/train_hand_net.py --phase 2` 가 자동으로 켠다:

| 손실 | 구현 | Phase 1 대비 변경 |
|---|---|---|
| `L_P-Chamfer` | `coverage_loss(partial=True)` | 양방향 → **단방향**. 로봇 여분 영역 왜곡 회피 |
| `L_dist` | `distance_loss` | **신규**. 손가락쌍 거리 보존 |
| `L_motion` | `motion_loss_local` | 글로벌 → **로컬**. 두 섭동의 사잇각을 맞춘다 |
| `L_align` | `align_loss` + 앵커 | **신규**. 비지도 → 약지도 전환의 핵심 |
| affine | `AffineHandNet` / `ResidualAffine` | `A=0`,`b=0` 초기화라 항등에서 시작 |

**검증된 성질 두 개** (`tests/test_hand_net_losses.py`):
- `motion_loss_local` 은 6D 랜덤 회전에 대해 **정확히 0**, `motion_loss_global` 은 아니다.
  AnyDexRT 의 "로컬이 캘리브레이션에 강건" 메커니즘이 실제로 성립한다.
- `chamfer_partial` 은 로봇 여분 영역을 무시(0.0)하고 `chamfer_both` 는 39 이상 벌금.

**앵커 = 캘리 재활용.** 사람 쪽은 theta `flat(0)` ↔ `fist(theta_hi 의 _flex 만)`,
로봇 쪽은 관절 샘플에서 `dorsum→distal` 평균거리가 최대/최소인 구성을 flat/fist 로
잡는다 — **손 구조와 무관하게 유도되므로 손마다 라벨링이 필요 없다.** 보간은 양쪽 다
물리 파라미터(theta / q)에서 하고 그 다음 encode 한다. keyvector 공간에서 직접
보간하면 물리적으로 불가능한 중간 자세가 섞인다.

**ablation**: P5a affine 유/무(`--no-affine`), P5b 전처리 정규화 유/무,
단방향 Chamfer 유/무(`--partial-chamfer`), 로컬 motion 유/무(`--local-motion`).
전부 플래그로 분리돼 있어 한 번에 하나만 바꿀 수 있다.

**미해결 위험 — 외전 축 앵커가 없다.** flat/fist 는 굴곡-신전 축만 덮는다. 그런데
Phase 1 이 가장 크게 실패한 곳이 벌림축(GMC −37.9)이고 논문 앵커 두 유형 중 하나가
"측면 회전"이다. `ref/planing.md` VIII-1 **splay 캘리 자세 추가 가능 여부가 Phase 2
성패를 가른다.** 안 되면 벌림축은 `L_align` 이 못 잡고 `L_motion`/`L_dist` 에만 의존한다.

**판정**: LMC 우선, GMC 병기, `|dq|p95`. 기준선은 §0.

## 6. Phase 3 — 셀슈바 백본 + 다중 임베디먼트  **(Phase 1·2 완료 후)**

`ref/planing.md` IV Phase 3 를 기준으로 하되, 목적함수는 아래로 확정한다.

```
L_backbone = λ1·L_smooth + λ2·L_contrastive(clean/noisy) + λ3·L_feasibility(joint_roms)
```

| 항 | 식 | 무엇을 위한 것인가 |
|---|---|---|
| `L_smooth` | `E_x[ ‖f_θ(x+δ) − f_θ(x)‖² / ‖δ‖² ]`, 작은 δ (Lipschitz 근사) | **few-shot 정당화.** 앵커 하나가 주변 이웃까지 대신 끌어주게 latent 를 부드럽게 정렬한다 |
| `L_contrastive` | `InfoNCE(z_clean, z_noisy_same_pose)` vs negative(다른 pose) | **VLA 정당화.** 노이즈 성분이 latent 에 새지 않게 — latent 는 순수 기구학적 자세만 담는다 |
| `L_feasibility` | 아래 참조 | 도달 불가 영역으로의 매핑 억제 |

**빠지는 것**: masked reconstruction, 뼈길이 불변항. 두 정당화 어느 쪽도 직접 요구하지
않는다. **남는 것**: temporal smoothness (VLA 도 시퀀스 예측이라 공통 요구).

**두 정당화는 서로 긴장 관계다.** 글러브 센서 노이즈(드롭아웃·EM 간섭)에 강하게 적응시키면,
VLA 가 나중에 내는 (노이즈 없는) 키포인트 예측과 latent 사이에 분포 괴리가 생긴다.
즉 강건성을 위한 노이즈 aug 가 VLA 연결 시 독이 될 수 있다. `L_contrastive` 는 이걸
"노이즈 강건성은 유지하되 latent 표현은 노이즈 무관"으로 분리하는 장치다.

**중복 위험 3건 — 가중치를 낮게 시작한다.**
1. `L_metric`(latent 위의 pairwise 거리보존)은 Phase 2 의 `L_dist`(keyvector 위)와 겹친다.
   keyvector 가 이미 6D 상대구조라 latent 에 또 얹는 건 중복 정규화일 수 있다 → **기본 제외**,
   ablation (d) 에서만 켠다.
2. `L_smooth` 는 **GeoRT flatness 와 겹친다.** flatness 는 `(f(x+δ)+f(x−δ)−2f(x))²` 로
   2차차분이고 둘 다 이미 구현돼 있다. 차이는 출력단(정확 FK 통과) vs latent 단이다.
   출력단 항이 이미 있는 상태에서 추가 이득이 있는지부터 잰다.
3. Phase 1 실측에서 **flatness 가 1e-10 수준**으로 사실상 0 이었다 — 이 계열 항이
   현재 스케일에서 학습에 기여하지 않는다는 신호다. `L_smooth` 도 같은 함정에 빠질 수 있으니
   δ 크기와 가중치를 먼저 검증한다.

**`L_feasibility` 는 재해석이 필요하다.** 출력이 `tanh → joint_roms` 선형 매핑이라
q 는 **구조적으로 항상 범위 안**이다 → q 에 대한 feasibility 페널티는 공허하다.
의미 있는 형태는 **tanh 포화 억제**다: `E[ relu(|pre_tanh| − c)² ]`. 포화하면 그래디언트가
죽고 관절이 한계에 달라붙는다(팔 IK 에서 joint5 가 가동범위 하한에 45% 프레임 붙어 있던
것과 같은 실패). 이걸 `L_feasibility` 로 쓴다.

**필수 ablation** (이게 없으면 정당화가 아니라 가설이다). **같은 gradient step 수로 맞추고**
few-shot 앵커 학습까지 끝낸 뒤 LMC 를 비교한다:

| | backbone pretext | 확인하는 것 |
|---|---|---|
| (a) | 없음 (Phase 2 손실만, backbone 포함 end-to-end) | 기준선 |
| (b) | `+ L_smooth` | 매끄러움이 few-shot 효율을 올리는가 |
| (c) | `+ L_smooth + L_contrastive` | 노이즈 불변이 추가 이득이 있는가 |
| (d) | `+ 전부 + L_metric` | 중복 정규화인지 확인 |

**(b) 가 (a) 를 못 이기면 backbone pretext 는 기각하고 Phase 2 상태를 최종으로 둔다.**

**두 번째 필수 실험**: **앵커 개수(5/10/20/50) 대비 LMC 곡선을 백본 유/무로 비교.**
이게 없으면 "백본 없이 손마다 5~10분 학습하면 되는데 왜?"에 답할 수 없다. 백본의 가치는
품질이 아니라 **로봇이 몇 대든 공유된다**는 것이므로, 곡선의 기울기가 근거가 된다.

## 7. 규칙

- 한 번에 하나만 바꾸고 ablation. 측정 전에 단정하지 않는다.
- 남의 논문 수치는 우리 하네스로 재현 전까지 설계 근거로 쓰지 않는다
  (AnyDexRT 의 LMC 우위는 저자 정의·자체 재구현 비교이고 핵심 ablation 이 한 손뿐이다).
- 기각도 근거와 함께 남긴다. 목표 기반 지표로 백엔드를 비교하지 말 것(과거 두 번 틀렸다).
