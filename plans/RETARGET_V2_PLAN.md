# 리타게팅 v2 실행 계획

방법론 정본은 `ref/planing.md`. 이 문서는 **실행 상태**만 기록한다 — 무엇이 끝났고,
무엇이 새 방법론으로 폐기됐고, 다음에 무엇을 어떤 판정 기준으로 재는가.

작업 브랜치 `feat/geort-retarget`. kp 구현은 `feat/kp-hand-retargeter` 에 고정(비교 기준선).
실험 코드는 저장소 밖 `/home/whatslab09/geort-lab` (GeoRT 벤더링, CC-BY-NC).

---

## 0. 확정 기준선 — **왼손** (`calib_base_left_20260810_154406.json`)

`tools/bench_hand_retarget.py --traj pinch|flex|abd`. `cos` 는 평균 코사인, `GMC%` 는 부호 일치율
(둘 다 전역 방식 — LMC 는 아직 미구현).

| 궤적 | 설정 | 형상° | 벌림mm | 핀치접촉 | 펴짐접촉 | GMC% | cos | \|dq\|p95 | ms |
|---|---|---|---|---|---|---|---|---|---|
| pinch | orca kp | 20.1 | 2.9 | 8.1 | 1.7 | 100.0 | 93.2 | 0.073 | 2.3 |
| pinch | orca dex | 12.3 | 6.0 | 8.2 | 32.9 | 96.1 | 87.9 | 0.139 | 5.8 |
| pinch | robotis kp | **34.1** | 3.7 | 10.1 | 3.3 | 100.0 | 85.6 | 0.071 | 2.0 |
| pinch | robotis dex | 13.6 | 9.4 | 33.3 | 38.3 | 78.9 | 53.0 | 0.175 | 6.4 |
| flex | orca kp | 32.5 | 10.7 | – | 1.1 | 98.9 | 89.6 | 0.151 | 2.3 |
| flex | orca dex | 33.9 | 10.9 | – | 34.7 | 96.8 | 91.6 | 0.122 | 7.1 |
| flex | robotis kp | 31.0 | 8.6 | – | 3.5 | 98.9 | 83.0 | 0.141 | 1.9 |
| flex | robotis dex | 17.6 | 11.4 | – | 32.2 | 89.5 | 78.9 | 0.229 | 6.9 |
| abd | orca kp | 23.9 | 3.0 | – | 11.2 | 93.4 | 88.1 | 0.054 | 2.4 |
| abd | robotis kp | **36.8** | 4.1 | – | 5.7 | 100.0 | 98.7 | 0.038 | 1.9 |
| abd | robotis dex | 27.2 | 6.0 | – | 23.3 | 100.0 | 82.8 | 0.197 | 6.6 |

**굽힘 전용 궤적이 미해결 문제를 드러낸다** — orca kp 이 핀치에서 벌림오차 2.9mm 인데
굽힘 전용에서 10.7mm, 형상 20.1 → 32.5° 로 무너진다. 굽힘만 하는 입력인데 손가락 간격이
사람을 못 따라간다 = "굽힐 때 좌우로 흔들린다"의 정량화.

**robotis 왼손이 오른손보다 크게 나쁘다** (참고용 오른손 값: 핀치 형상 15.3°, 벌림축 17.5°
→ 왼손 34.1°, 36.8°). §1.1 의 좌우 URDF 비대칭이 리타게팅 품질에 그대로 나타난 것으로 본다.
왼손 기준선이 정본이지만, robotis 왼손 수치는 URDF 수정 후 다시 재야 한다.

### 0.1 좌우 미러 실측 (통합 판단의 전제)

같은 q 를 좌우에 넣고 팜프레임 로컬 사슬 좌표를 `(x, y, -z)` 로 비교한 값.

| | 중립 사슬 미러오차 | 관절별 미러오차 >2mm | 최악 |
|---|---|---|---|
| human | – | 4/21 (`{index,middle,ring,pinky}_mcp_abd`) | 49.6mm (15°) / 81mm (25°) |
| orca | 0.03mm | **3/16** (전부 외전축 `*_to_Carpals`) | 8.53mm (그 관절 변위 22.2mm 중) |
| robotis | 9.79mm (전 손가락 균일) | **20/20** | 69.6mm (그 관절 변위 28.8mm 중) |

이전 세션에 기록한 "robotis 는 완전한 미러(0.0mm) / orca 는 35~45mm 어긋남"은 **틀렸다** —
지문 위치만 옛 URDF 로 잰 값이었다. 위 표가 정본이다.

**이전 세션 표(형상 43.3° 등)는 폐기.** 사람 URDF 관절명이 구버전(`mcp_z`/`mcp_y`/
`thumb_cmc0`)이라 프로파일과 13/21 이 어긋난 상태에서 측정한 값이다. 판정 기준으로
쓰지 말 것 — Phase 1 의 "43.3° → 20°" 목표도 새 기준선으로 다시 잡아야 한다.

---

## 1. 완료

- **`tools/bench_hand_retarget.py`**
  - 변위 방향 일치 지표 추가. **현재 구현은 GMC 다** — 사람·로봇 변위를 각각 고정된
    중립 팜 프레임에서 비교하는 전역 방식. `ref/planing.md` VI 의 LMC(로컬 프레임)는
    아직 없다. 표의 `cos` 는 평균 코사인, `GMC%` 는 부호 일치율.
  - 부호 일치율만 보면 kp 이 양손 다 100% 로 포화한다(핀치 램프가 단조) → 평균 코사인
    병기가 필수.
  - `--traj pinch|flex|abd`. 프로파일 theta 가 `{finger}_flex`/`{finger}_abd` 로 분리돼
    있어 덤프 `theta_lo`/`theta_hi` 로 굽힘·벌림 전용 스윕을 합성한다.
- **사람 URDF 관절명 갱신** — Visualizer 현행 URDF(`mcp_flex`/`mcp_abd`/`thumb_cmc_flex`)
  로 models root 재생성. 프로파일 21개 관절 전부 매칭, 테스트 116 passed / 6 skipped.
- **GeoRT 벤더링 + 키포인트 차원 일반화 패치**(`geort-lab/patch_kp_dim.py`) —
  `config["keypoint_dim"]` 으로 IK/FK 넷 입출력 차원을 바꿀 수 있게 했다. Phase 1 에서
  재사용하지만 **신경 FK 자체는 pinocchio 로 교체되므로 FK 쪽 패치는 버려진다.**

## 2. 새 방법론으로 폐기된 것

- **f_s 3안 비교(NNS / 학습IK / kp DLS)** — 통합 구조(F 가 q 직접 출력)라 해당 없음.
  측정해 둔 NNS 특성(사전샘플 20배에 오차 19.2→14.8mm, 1↔2위 이웃 q 점프가 밀도와
  무관하게 1.1rad 고정)은 **NNS 를 쓰지 않는 근거**로만 남긴다.
- **`sensor_proximal` 프레임을 prox 로 쓰는 것** — prox 는 **사슬 50% 중앙**으로 바뀐다.
  센서 실장 위치는 손마다 통일 작업이 반복돼 임베디먼트 확장에 약하다.
- **`v_tip→prox` keyvector** — `v_dorsum→{distal,prox}` 로 대체(선형종속 제외).
- **진행 중이던 tip/tip_prox 학습 런** — 키포인트 정의와 FK 가 둘 다 바뀌어 무효.
  (두 번 세션 경계에서 죽기도 했다. 재실행은 새 정의로.)

## 2.5 좌우 모델을 통합할지 — 결론: **통합을 목표로 설계, 도입은 미러 게이트 통과 후**

**통합이 가능한 근거.** 좌우 URDF 가 참 미러라면 팜프레임 로컬에서 왼손 keyvector 는
오른손 keyvector 의 z 성분 부호를 뒤집은 것과 **정확히** 같다(§0.1 의 비교식이 그것이다).
관절 한계도 좌우 동일하고 관절명은 접두사만 다르다. 따라서

```
q_robot = F( mirror_z(keyvector_left) )     # 같은 F, 같은 q
```

가 근사가 아니라 **엄밀한 대칭**이다. 같은 q 를 미러 기구에 넣으면 미러 동작이 나온다는 것을
§0.1 이 측정으로 확인했다(미러가 성립하는 관절에서 오차 0.00mm).

**통합해야 하는 이유 — 대칭성이 공짜로 보장된다.** 비지도 학습은 해가 유일하지 않아
시드마다 흔들린다(GeoRT 는 Ability Hand LMC 36.2±11.2 를 보고했다). 좌우를 **따로**
학습하면 두 손이 서로 다른 국소해에 앉아 **조작자가 왼손과 오른손에서 다른 감각**을 느낀다.
지금 겪는 흔들림과 결이 같은 문제이고, 통합은 이걸 구조적으로 없앤다. 부수 이득으로
학습 데이터가 2배(좌우 글러브 시퀀스를 미러로 합침), 체크포인트·학습시간은 절반이 된다.

**그런데 지금은 도입할 수 없다.** §0.1 대로 human 4/21, orca 3/16, robotis 20/20 관절이
미러가 아니다. 미러가 깨진 상태에서 통합하면 한쪽 손의 오차를 다른 쪽에 강제로 떠넘긴다.

**따라서**: Phase 1 은 **side 별 모델**로 진행하고(기준선이 왼손이므로 왼손 우선),
`tools/check_mirror.py` 를 만들어 위 표를 자동 생성한다. **손 하나가 전 관절 미러오차
2mm 이하가 되면 그 손만 통합 모델로 전환**한다. orca 는 외전축 3개만 남아 먼저 열릴 가능성이
높고, robotis 는 20/20 이라 시간이 걸린다 — 손별로 따로 판단한다.

**미러가 영구히 안 되는 손이 나오면** (설계상 좌우가 진짜 다른 경우) 그 손만 side 별
모델을 유지한다. 통합은 최적화이지 전제가 아니다.

## 3. P0 — 이후 모든 판정의 전제

| | 항목 | 담당 | 판정 |
|---|---|---|---|
| P0-1 | dorsum 원점 통일 (6개 URDF) | URDF(Visualizer) | 좌우 미러오차 0.00mm, 손간 규약 일치 |
| P0-2 | **LMC** 구현 + GMC 병기 | 코드 | 두 값의 격차가 캘리 품질 진단이 되도록 |
| P0-3 | pinocchio FK 배치 성능 | 코드 | 학습 배치에서 병목인지. 병목이면 pytorch_kinematics 병행 후 결과 대조 |

**P0-1 잔여 이슈 (URDF 쪽, 코드로 보정하지 않는다)**
1. **오른손 `*_mcp_abd` 4개 축이 미러가 아니다.** 좌우 모두 `axis="0 0 1"` 인데 축은
   의사벡터라 미러 시 `a' = -M a` 로 부호가 바뀌어야 한다. 실측 미러오차 15° 에서
   index 43.9 / middle 49.6 / ring 43.0 / pinky 35.4mm, 25° 에서 최대 81mm.
   오른손 4개만 반전하면 5·15·25° 전부 **0.00mm**. 엄지 5축과 flex/pip/dip 는 정상.
   현재 벤치는 오른손 단일 side 라 이 버그와 무관하지만, 좌우 공용 학습에는 막힘돌이다.
2. **dorsum 위치가 손마다 다르다** (`ref/planing.md` III 표). robotis 좌우 50mm 불일치.
3. 동봉 패키지 `dexhand_description` 은 여전히 구버전 관절명 → `WHATSLAB_MODELS_ROOT`
   를 반드시 건다. 패키지 릴리스 시 갱신 필요.

## 4. Phase 1 — GeoRT + prox, pinocchio 정확 FK

구현 순서:
1. `ref/planing.md` III 전처리로 keyvector 재정의 — 원점 dorsum, 축 손가락방향 유도,
   prox = 사슬 50% 중앙, `v_dorsum→{distal,prox}` 6D, `L_ref` 상수(identity q=0),
   데이터셋 통계 정규화 없음.
2. **`torch.autograd.Function` 으로 pinocchio FK 랩** — `∂L/∂q = (∂L/∂x)^T · J`.
   신경 FK 제거. 추론 경로에 FK 없음(forward 1회).
3. F = 손가락별 MLP `(6, 128, 128, n_joints_i)`, tanh → `joint_roms` 선형 매핑.
4. 손실은 GeoRT 5원칙 유지(motion / C-space coverage / flatness / pinch / collision).
5. 학습 데이터: 합성(사람 URDF 랜덤) 사전학습 → 글러브 실시퀀스 파인튜닝 2단계.

**판정**: 형상 오차. 목표치는 §0 새 기준선으로 다시 정한다(구 43.3° 는 무효).
**부수 확인**: `|dq|p95`. 시간축이 없으므로 kp 0.072 를 넘기기 어렵다는 것이 사전 예측이고,
대응은 ① EMA 입력 필터 → ② Lipschitz 페널티 → ③ 과거 사람 입력 윈도우(k=2~3) 순서다.
자기 출력 되먹임은 드리프트 위험이라 쓰지 않는다.

## 5. Phase 2 — AnyDexRT 4손실 + anchor + residual affine

- `L_P-Chamfer`(양방향→단방향) / `L_dist`(신규) / `L_motion`(글로벌→로컬 프레임) /
  `L_align`(few-shot anchor).
- F 에 손가락별 residual affine(`A=I`, `b=0` 초기화) 추가.
- ablation: P5a affine 유/무, P5b 전처리 정규화 유/무.
- anchor 는 캘리(flat/fist/pinch) 재활용. **외전 축 anchor 가 없다** — `ref/planing.md`
  VIII-1 splay 자세 추가 가능 여부가 선행 미결정이고, 미해결 문제가 외전 흔들림이므로
  이게 Phase 2 성패를 가른다.
- 핀치 소프트 anchor 는 P5-1 수렴 후 warm-up 으로만 활성화(부트스트랩 순환 차단).

**판정**: LMC 우선, GMC 병기, `|dq|p95`.

## 6. Phase 3 — 셀슈바 백본 + 다중 임베디먼트

`ref/planing.md` IV Phase 3 그대로. **필수 실험은 앵커 개수(5/10/20/50) 대비 LMC 곡선을
백본 유/무로 비교**하는 것 하나다 — 이게 없으면 "백본 없이 손마다 5~10분 학습하면
되는데 왜?"에 답할 수 없다.

## 7. 규칙

- 한 번에 하나만 바꾸고 ablation. 측정 전에 단정하지 않는다.
- 남의 논문 수치는 우리 하네스로 재현 전까지 설계 근거로 쓰지 않는다
  (AnyDexRT 의 LMC 우위는 저자 정의·자체 재구현 비교이고 핵심 ablation 이 한 손뿐이다).
- 기각도 근거와 함께 남긴다. 목표 기반 지표로 백엔드를 비교하지 말 것(과거 두 번 틀렸다).
